function dijkstra(gridMatrix, startNode, endNode){
    let nodeCount = grid.length * grod[0].length;
    let distance = Array.from(Array(gridMatrix.length), _ => Array(gridMatrix[0].length).fill(Infinity));
    distance[start[0]][start[1]] = 0;
    console.log("dijkstra");
}